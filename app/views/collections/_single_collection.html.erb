<div class="container">
  <p>
    <strong><%= t 'collections.show.description' %></strong>
    <%= @collection.content %>
  </p>
  <p>
    <strong><%= t 'collections.show.theme' %></strong>
    <%= @collection.theme_text %>
  </p>
  <% if !@collection.picture.file.nil? %>
    <p>
      <%= image_tag @collection.picture.url %>
    </p>
  <% end %>
</div>
