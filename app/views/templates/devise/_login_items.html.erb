<% if user_signed_in? %>
  <li class="nav-item p-2">
  <%= link_to((t 'templates.logout'), destroy_user_session_path, method: :delete) %>        
  </li>
<% else %>
  <li class="nav-item p-2">
  <%= link_to((t 'users.shared.log_in'), new_user_session_path) %>  
  </li>
<% end %>
